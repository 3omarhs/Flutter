import 'package:flutter/material.dart';

class HorizantalSpace extends StatelessWidget {
  const HorizantalSpace({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      width: 20,
    );
  }
}
